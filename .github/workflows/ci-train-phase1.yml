name: CI train phase1 gate (legacy manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain legacy gate
        run: |
          echo 'Phase1 automatic gate is retired.'
          echo 'Use CI train phase2 gate for state-only finalization.'
