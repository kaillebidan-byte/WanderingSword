# 現在の申し送り

> 新しいチャットが過去会話なしで現在地を復元するための入口。機械可読の正本は `CURRENT_WORK.json`、次作業の具体的な着眼点と所有表は `NEXT_TASK_PACKET.json`、品質段階と件数は `audit_status.json`。`_handover.md` は履歴であり、現在地として使わない。

## 新チャットで送る一文

同じChatGPTプロジェクト内では、リポジトリURLや前回作業を聞き直さず、次の一文だけで再開する。

```text
現状把握して作業の続きを
```

受けた側は、未統合PR、GitHub Actions、main、状態文書、最新artifact、既存修正束の実所有を照合する。短い報告だけで終わらず、同じ応答内で実作業へ進む。現状だけ必要な場合はユーザーが明示する。

## 現在地

- checkpoint: `verified`
- checkpointを生成した翻訳PR: #80（統合済み）
- 直近の統合済み翻訳PR: #80
- superseded PR: #74・#75
- クラスタ: 武当師門中核
- 人物ペア: 宇文逸↔莫問
- 段階: 既訳再監査を継続中
- 完了: 第49束
- 宇文逸↔莫問の適用キー: 1139
- プロジェクト全体の適用キー: 1423
- 最新pak: `_work/aaWanderingSword_JP_P.pak`
- build: 検証済み・ゲーム未配置
- game verification: 未開始

再開時に未統合PRがあれば、まず `active / superseded / abandoned / unrelated` に分類する。active PRがなければ、`CURRENT_WORK.immediate_next` と `NEXT_TASK_PACKET.json` から第50束へ入る。PRは開いているだけで現行作業と決めない。

## 第49束で完了したこと

`5303_1` の8行を通読し、6キーを再監査した。

- 人物ペア新規1キー: 宇文逸Index0
- 既存第33束再改訂5キー: Index1・2・5・6・7
- 変更なし2キー: Index3・4
- 人物ペア累計1139、全体1423
- locres反映、pak再生成、全1423キー差分0、register lint、関係抽出、単体テスト、回帰走査、pak実体・LFS確認済み

主な修正は次のとおり。

- `原来` の気づきを落とさず、`そうか……莫問師兄も私と同じで` と独白を始めた
- `好在` を説明語の `幸い` に固定せず、直前の境遇との対比を `でも、私には叔父さんがいて` へ移した
- 叔父への `不怎么靠谱` を侮蔑へ強めず、`時々ちょっと頼りない` という軽い愚痴に留めた
- 莫問への感謝を `いつもこんなに世話になっている` と継続的な関係の心内語へ戻した
- `听起来就很热闹` の賑わいを `聞いているだけでも賑やかそうだ` と保持した
- 大会参加を確定せず、修練意欲を `私もこれから、もっと腕を磨かないと！` と独立して出した
- `找元风师兄` の硬い `訪ねる` を、`元風師兄のところへ行って` と寝る前の独白へした

ALLUSION_REVIEW候補はない。両親の死亡・失踪、叔父の恒常的な無責任、莫問の好み、贈り物の具体物、品剣大会への参加・見物、翌日の任務内容は補っていない。

## 所有境界の反例と制度改修

旧 `NEXT_TASK_PACKET.json` はIndex1・2・3・5・6・7を「既存第4束所有」としていたが、実際の所有者は `_phase4_proofread/fixes_relation_yuwen_mowen_20260724_batch33.json` だった。人物資料や過去の自由記述ではなく、修正JSONを一次資料として訂正した。

再発防止として次を追加した。

- `NEXT_TASK_PACKET.json` schema v2の `ownership_boundary.machine_ownership`
- 既存所有キーの実ファイルパスと、未所有キーの予定所有先
- `check_next_task_packet.py` による全 `fixes_*.json` の走査
- 所有元の不一致、未所有誤認、複数所有、focus keyの未被覆・重複の検出
- 同一PR内で未所有候補が宣言済みの `planned_owner` へ収録された場合だけ、正規の遷移として許容
- Relation auditとApply CIへ所有検査の回帰テストを接続

翻訳判断は既存skillで扱えたため、skill本体と人物資料は変更していない。所有誤記は再開・CI制度の問題として修正した。

## 次の校正

active PRがなければ、`5331_1`・`5343_4` の20行を第50束として監査する。

- 名剣山荘の招待状を読む引用声と、古剣《湛盧》の重み
- 品剣大会が六扇門の天龍幇対策であるという清虚の推測
- 宇文逸の仇討ちと大会参加願望
- 門内大比三位以内という参加条件と、莫問への伝達
- 門内大比後の `幸不辱命` による復命
- 莫問が不参加だったため勝てたという宇文逸の謙遜
- 清虚による若手の称揚と、宇文逸の武当侠義継承の誓い

実所有は機械表に固定した。

- 既存第34束: 宇文逸6キー（`5331_1` Index3・7・10、`5343_4` Index0・3・7）
- 未所有の宇文逸: `5331_1` Index6。変更時は新規第50束へ
- 未所有の清虚: 13キー。変更時は清虚cross-registerへ分離し、宇文逸↔莫問の新規キー数へ算入しない

招待状の `鄙庄` を清虚自身の「我が荘」へ誤帰属させない。`很可能／应该／看来` は清虚の推測として保ち、六扇門の密偵情報、大会参加、湛盧の授与、莫問不参加の理由を確定しない。

具体的な全キー、ALLUSION_REVIEW、FACT_DOUBT、機械所有表、完了条件は `NEXT_TASK_PACKET.json` に固定した。自由記述と機械所有表が食い違う場合は、実修正JSONと機械検査を優先して資料側を直す。

## checkpointと遷移状態

- `verified`: 状態文書、監査索引、件数、適用記録、次作業パケットが同期済み。統合可能。
- `pending_audit_sync`: 翻訳適用後、botの監査索引書き戻しと最終状態確定を待つ遷移状態。作業続行は可能だが統合禁止。
